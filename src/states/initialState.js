//initialState is the shape of information we need in the reducer
const initialState = {
    loading: false,
    error: "",
    searchValue: "",
    searchValue2: "96822"
}
export default initialState